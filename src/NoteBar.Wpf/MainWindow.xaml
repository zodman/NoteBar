<Window x:Class="NoteBar.Wpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="NoteBar"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        WindowStartupLocation="Manual"
        SizeToContent="WidthAndHeight"
        MouseLeftButtonDown="Window_MouseLeftButtonDown">

    <Border Background="#F21E1E22" 
            BorderBrush="#55FFFFFF" 
            BorderThickness="1" 
            CornerRadius="14" 
            Padding="8,4,8,4" 
            Margin="8">
        <Border.Effect>
            <DropShadowEffect BlurRadius="12" ShadowDepth="2" Opacity="0.5" Direction="270"/>
        </Border.Effect>

        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
            <!-- Grip and App Title -->
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Cursor="SizeAll" ToolTip="Click and drag to move NoteBar">
                <TextBlock Text="⋮⋮" Foreground="#66FFFFFF" FontSize="12" VerticalAlignment="Center" Margin="0,0,4,0"/>
                <TextBlock Text="NoteBar" Foreground="#AAFFFFFF" FontSize="11" FontWeight="SemiBold" VerticalAlignment="Center" Margin="0,0,6,0"/>
            </StackPanel>

            <Rectangle Width="1" Height="14" Fill="#33FFFFFF" Margin="0,0,6,0" VerticalAlignment="Center"/>

            <!-- If no indicators are loaded, show starter prompt -->
            <StackPanel x:Name="EmptyPanel" Orientation="Horizontal" VerticalAlignment="Center" Margin="2,0,2,0">
                <Ellipse Width="8" Height="8" Fill="#00D2FF" Margin="2,0,6,0" VerticalAlignment="Center"/>
                <TextBlock Text="Ready (UDP 1738)" Foreground="#888888" FontSize="11" VerticalAlignment="Center" Margin="0,0,4,0"/>
            </StackPanel>

            <!-- Indicators Collection -->
            <ItemsControl x:Name="IndicatorsControl" ItemsSource="{Binding Indicators}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center" />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Grid x:Name="IndicatorGrid" Cursor="Hand" Margin="2,0,2,0" VerticalAlignment="Center">
                            <Border x:Name="HoverBorder" 
                                    CornerRadius="8" 
                                    Padding="4,2,4,2"
                                    Background="Transparent">
                                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                    <Image Source="{Binding IconImage}" 
                                           Width="18" Height="18" 
                                           RenderOptions.BitmapScalingMode="HighQuality" 
                                           VerticalAlignment="Center"/>
                                    <TextBlock Text="{Binding Port}" 
                                               Foreground="#DDFFFFFF" 
                                               FontSize="11" 
                                               FontWeight="SemiBold" 
                                               VerticalAlignment="Center" 
                                               Margin="4,0,2,0"/>
                                </StackPanel>
                            </Border>

                            <Grid.ToolTip>
                                <ToolTip Content="{Binding Port, StringFormat='NoteBar: Port {0} (Click &amp; drag to move, right-click for options)'}" />
                            </Grid.ToolTip>

                            <Grid.ContextMenu>
                                <ContextMenu>
                                    <MenuItem Header="{Binding Port, StringFormat='UDP Port: {0}'}" IsEnabled="False" FontWeight="Bold" />
                                    <MenuItem Header="{Binding CurrentIconName, StringFormat='Status: {0}'}" IsEnabled="False" />
                                    <Separator />
                                    <MenuItem Header="Close Indicator" Click="MenuItemQuit_Click" />
                                    <Separator />
                                    <MenuItem Header="Center Bar on Screen" Click="MenuItemCenterPos_Click" />
                                    <MenuItem Header="Reset Bar Position" Click="MenuItemResetPos_Click" />
                                    <MenuItem Header="Hide Floating Bar (Keep in Tray)" Click="MenuItemHideBar_Click" />
                                    <Separator />
                                    <MenuItem Header="Exit NoteBar" Click="MenuItemExit_Click" />
                                </ContextMenu>
                            </Grid.ContextMenu>
                        </Grid>

                        <DataTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="HoverBorder" Property="Background" Value="#33FFFFFF" />
                            </Trigger>
                        </DataTemplate.Triggers>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </StackPanel>
    </Border>
</Window>
